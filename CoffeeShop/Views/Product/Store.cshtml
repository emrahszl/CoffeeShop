@*@model List<Product>
@{

}

<section class="page-section cta">
    <div class="container">
        <div class="row">
            <div class="col-xl-9 mx-auto">
                <div class="cta-inner bg-faded text-center rounded">
                    <table class="table table-hover table-striped">
                        <thead>
                            <tr>
                                <th>Id</th>
                                <th>Product Name</th>
                                <th>Product Price</th>
                                <th>Product Image Name</th>
                                <th>Product Stock Amount</th>
                                <th></th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (Product product in Model)
                            {
                                <tr>
                                    <td>@product.Id</td>
                                    <td>@product.ProductName</td>
                                    <td>@product.ProductPrice</td>
                                    <td><img src="~/products/img/@product.ProductImageName" alt="Alternate Text" /></td>
                                    <td>@product.ProductStock</td>
                                    <td>
                                        <a class="btn btn-danger" asp-controller="Product" asp-action="Remove" asp-route-id="@product.Id">Remove</a>
                                        <a class="btn btn-warning" asp-controller="Product" asp-action="Update" asp-route-id="@product.Id">Update</a>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</section>*@
